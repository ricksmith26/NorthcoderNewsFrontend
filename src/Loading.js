import React from 'react';
import Loading from 'react-loading-components';

const LoadingIcon = () => (
  <Loading type="ball_triangle" width={100} height={100} fill="#941010" />
);

export default LoadingIcon;
